/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'assets.aceternity.com','pbs.twimg.com'], // Add the Unsplash domain here
  },
};

export default nextConfig;
